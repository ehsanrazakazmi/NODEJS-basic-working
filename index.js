import  getpost, {getPostLength} from "./postController.js";

console.log(getpost());

console.log(getPostLength());
// const {generateRandomNumber, cleciusTofarenheight} = require('./utils');

// console.log(`random ${generateRandomNumber()}`);

// console.log(`c to f ${cleciusTofarenheight(45)}`);